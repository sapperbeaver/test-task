import {Action, createAction, createReducer, on, props, State} from '@ngrx/store';
//
// const initialState = {
// };
// export const init = createAction('[app] init', props<{state: any}>());
// const appReducer = createReducer(
//   initialState,
//   on(init, (state, payload) => {
//     console.log(payload);
//     return payload.state;
//   }),
// );
//
// export function reducer(state: {} | undefined, action: Action) {
//   return appReducer(state, action);
// }
